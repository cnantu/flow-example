import DropTargetNode from "./DropTarget";
import ExitNode from "./ExitNode";
import ButtonEdge from './ButtonEdge';

export const nodeTypes = {
  dropTarget: DropTargetNode,
  exit: ExitNode,
};

export const edgeTypes = {
  buttonedge: ButtonEdge,
};